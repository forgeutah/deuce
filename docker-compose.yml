services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: deuce
      POSTGRES_USER: deuce
      POSTGRES_PASSWORD: deuce
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
